# ==============================================================================
# NUGI CONTENT FACTORY - ENVIRONMENT CONFIGURATION TEMPLATE
# ==============================================================================

# Application Environment (development | testing | production)
APP_ENV=development
APP_NAME="Nugi Content Factory"
DEBUG=true

# Server Configuration
HOST=127.0.0.1
PORT=8000
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000

# Database Connection (SQLite by default for zero-setup, PostgreSQL ready)
DATABASE_URL=sqlite:///./nugi_content_factory.db
# PostgreSQL Example: postgresql+psycopg2://user:password@localhost:5432/nugi_content_factory

# File Storage Configuration
STORAGE_PROVIDER=local
STORAGE_BASE_DIR=./storage/assets

# AI Provider Configuration ('mock' by default, or 'openrouter' / 'flux')
LLM_PROVIDER=mock
IMAGE_PROVIDER=mock

# OpenRouter LLM Configuration (When LLM_PROVIDER=openrouter)
OPENROUTER_API_KEY=
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_MODEL=google/gemini-2.5-flash-lite

# Flux / Black Forest Labs API Configuration (When IMAGE_PROVIDER=flux)
FLUX_API_KEY=
FLUX_MODEL=flux-2-klein-9b
FLUX_BASE_URL=https://bfl.ai

# Optional Alternate LLM API Keys (OpenAI / Anthropic)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Rendering Configuration
DEFAULT_IMAGE_WIDTH=1080
DEFAULT_IMAGE_HEIGHT=1080
DEFAULT_FONT_FAMILY=sans-serif
